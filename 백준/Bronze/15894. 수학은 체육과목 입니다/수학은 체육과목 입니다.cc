#include <iostream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);

    long long n;
    cin >> n;

    // 4n, 4n + 4, 6n + 61

    cout << 4*n << endl;

    return 0;
}